services:
  api:
    command: npm run dev
    volumes:
      - ./api/src:/app/src
      - /app/node_modules
    restart: "no"
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
